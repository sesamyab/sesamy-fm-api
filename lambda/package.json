{
  "name": "sesamy-encoding-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda audio encoding service with FFmpeg",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "fluent-ffmpeg": "^2.1.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
